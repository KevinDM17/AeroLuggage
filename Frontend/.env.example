# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Puerto en el que corre el servidor de desarrollo de Vite.
PORT=5173

# URL base del backend (sin slash final). Ej: http://localhost:8080/api
BACKEND_API_BASE_URL=http://localhost:8080/api

# Host del WebSocket STOMP del back (sin /ws al final, eso lo agrega el cliente).
# El back monta el endpoint en /ws con SockJS.
BACKEND_WS_BASE_URL=http://localhost:8080

# Cuando esta en "true" el front usa datos en memoria (sin necesidad del back).
# Cuando esta en "false" o vacio, hace fetch REST + se conecta al WS STOMP.
VITE_USE_MOCK=false

# Intervalo de polling en ms (solo para endpoints REST que aun no estan via WS).
VITE_POLL_INTERVAL_MS=5000
